# Public browser port.
OPERAI_PORT=8080

# Exported DINOv3 ONNX model on the Docker host. FP32 is the accuracy-first
# default. Use the INT8 artifact after validating its predictions on the target.
OPERAI_MODEL_PATH=./models/operai_eye_dinov3.onnx
